#version 450

layout (binding = 0, r16) uniform image2D heightMapTexture;

layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

void main()
{
    imageStore(heightMapTexture, ivec2(gl_GlobalInvocationID.xy), 
		vec4(float(gl_GlobalInvocationID.x + gl_GlobalInvocationID.y) / 1024.0, 0, 0, 0));
}