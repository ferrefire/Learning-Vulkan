#version 450

layout (binding = 0, rgba8) uniform image2D heightMapTexture;

layout (local_size_x = 512, local_size_y = 512, local_size_z = 1) in;

void main()
{
    imageStore(heightMapTexture, ivec2(gl_GlobalInvocationID.xy), vec4(1));
}